// Replaced at build time by scripts/build.mjs with the gzip+base64 payload
// and a streaming DecompressionStream-based loader.
export const scope = '.ds-247420';
export async function loadCss() { return ''; }
